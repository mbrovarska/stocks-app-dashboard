import React from "react";
import LoadingIcons from "react-loading-icons";

const Loading = () => {
  return (
    <div className="loading-message-container">
      <LoadingIcons.Circles width="50px" />
    </div>
  );
};

export default Loading;
